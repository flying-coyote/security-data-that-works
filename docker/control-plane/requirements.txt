# MOAR control-plane (marimo console) — pinned to the versions the lab venv runs.
# Reproduce the console env: python -m venv .venv && .venv/bin/pip install -r requirements.txt
marimo==0.23.9
pyiceberg==0.11.1
duckdb==1.5.3
pandas==3.0.3
pyarrow==24.0.0

